/**
 *@module
 * @category v
 */
import type { obj as obj1 } from './types'

/**
 * yeah buddy
 *@remark remarkable
 * @param a just an a
 * @throws {@link MyError} will throw
 */
export const func = (a: obj1) => {}
